/**
 * Custom Firebase interface adapter for the Empire Age world map system
 */

import { auth, db, handleFirestoreError, OperationType } from '../lib/firebase';

export { auth, db, handleFirestoreError, OperationType };
export default db;
